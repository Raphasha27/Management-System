name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
<<<<<<< HEAD

      - name: Install dependencies
        run: npm install

      - name: Build application
        run: npm run build
        env: 
        DATABASE_URL: "file:./dev.db" # Local SQLite for build check
        NEXT_PUBLIC_APP_URL: "http://localhost:3000"
  security_scan: 
  security_scan:
    name: Security Scan (Trivy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
=======
          cache: npm
      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Lint
        run: npm run lint --if-present || echo "No lint script"
      - name: Build
        run: npm run build --if-present || echo "No build script"
      - name: Test
        run: npm test --if-present -- --watchAll=false 2>/dev/null || echo "No test script"
>>>>>>> 16193d84873d9ee82f72017a19e77fc770ed82d0